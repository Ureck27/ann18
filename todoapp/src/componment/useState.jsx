import { useState } from "react";

const Button =({lable,onClick})=>{
    return <div onClick={onClick}>lable</div>
}

const EXo02 =()=>{
    const [y,o] =q ;
    const [g,q] =o ;
    const [a,b] = c;

}